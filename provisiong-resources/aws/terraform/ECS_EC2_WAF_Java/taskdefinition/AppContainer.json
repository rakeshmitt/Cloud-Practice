[
  {
    "name": "${app_name}",
    "image": "${app_image}",
    "cpu": ${app_cpu},
    "memory": ${app_memory},
    "essential": true,
    "portMappings": [
      {
        "containerPort": ${app_container_Port},
        "hostPort": ${app_host_port}
      }
    ]
  }
 ]